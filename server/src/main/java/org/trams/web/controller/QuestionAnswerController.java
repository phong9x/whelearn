/*
 * Created on 23 thg 9 2016 ( Time 15:40:53 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */
package org.trams.web.controller;

import java.util.List;
import java.util.ArrayList;
import java.util.Date;

import javax.annotation.Resource;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpSession;
import javax.validation.Valid;
import org.springframework.data.domain.Page;
import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RequestParam;
import org.springframework.web.servlet.mvc.support.RedirectAttributes;
import org.springframework.web.bind.annotation.ModelAttribute;


//--- Common classes
import org.trams.web.common.AbstractController;
import org.trams.web.common.Pager;
import org.trams.web.common.utils.DataUtils;
import org.trams.web.common.Login;
import org.trams.bean.jpa.QuestionAnswerEntity;

//--- Entities
import org.trams.bean.QuestionAnswer;

//--- Services 
import org.trams.business.service.QuestionAnswerService;

/**
 * Spring MVC controller for 'QuestionAnswer' management.
 */
@Controller
@RequestMapping("/questionAnswer")
public class QuestionAnswerController extends AbstractController {

	//--- Variables names ( to be used in JSP with Expression Language )
	private static final String MAIN_ENTITY_NAME = "questionAnswer";

	private static final Integer PAGE_SIZE   = 10;

	private static String nav = "questionAnswer";

	//--- JSP pages names ( View name in the MVC model )
	private static final String JSP_CREATE   = "questionAnswer/create";
	private static final String JSP_LIST   = "questionAnswer/list";
	private static final String JSP_EDIT   = "questionAnswer/edit";
	private static final String JSP_DETAIL   = "questionAnswer/detail";
	private static final String JSP_LOGIN   = "redirect:/login";
	private static final String MAIN_LIST_NAME   = "list";
	//--- Main entity service
	@Resource
    private QuestionAnswerService questionAnswerService; // Injected by Spring
	//--- Other service(s)

	//--------------------------------------------------------------------------------------
	/**
	 * Default constructor
	 */
	public QuestionAnswerController() {
		super(QuestionAnswerController.class, MAIN_ENTITY_NAME );
		log("QuestionAnswerController created.");
	}

	@RequestMapping("/list")
	public String list(
			@RequestParam(value="page",defaultValue="1") Integer page,
			@RequestParam(value="type",defaultValue="") String type,
			@RequestParam(value="key",defaultValue="") String key,
			@RequestParam(value="delete",defaultValue="0") Integer delete,
			HttpSession session,
			Model model) {
		if(key==null){
			key="";
		}
		if(type==null){
			type="";
		}
		if(delete>0){
			try {
				questionAnswerService.delete(delete);
			} catch (Exception e) {
				// TODO: handle exception
			}
		}
		Page<QuestionAnswerEntity> listPage = null;
		if(type == ""){
			listPage = questionAnswerService.listPaging(page, PAGE_SIZE);
		}
		model.addAttribute(MAIN_LIST_NAME, listPage);	
		model.addAttribute("key", key);
		model.addAttribute("type", type);
		model.addAttribute("activePage", nav);
		model.addAttribute("pagination_navigator", "/questionAnswer/list");
		new Pager<QuestionAnswerEntity>(listPage).setSetting(model, "type="+type+"&amp;key="+key+"&amp;");		
		return JSP_LIST;
	}

	
	@RequestMapping("/detail/{id}")
	public String detail(
	Model model, 
	HttpSession session,
	@PathVariable("id") Integer id) {
		QuestionAnswer questionAnswer = questionAnswerService.findById(id);
		model.addAttribute("questionAnswer", questionAnswer);
		model.addAttribute("activePage", nav);
		return JSP_DETAIL;
	}
	@RequestMapping(value = "/create", method = RequestMethod.GET ) 
	public String create(
			HttpSession session,
			Model model) {
		log("Action 'create'");
		model.addAttribute("activePage", nav);
		return JSP_CREATE;
	}

	@RequestMapping(value = "/create", method = RequestMethod.POST ) 
	public String create(
			HttpSession session,
			@ModelAttribute QuestionAnswer questionAnswer,
			Model model) {
		log("Action 'create'");
		try {
			questionAnswer.setCreateDate( DataUtils.getNowDate() );
			questionAnswer.setUpdateDate( DataUtils.getNowDate() );
			questionAnswerService.create(questionAnswer);
			return "redirect:/"+JSP_LIST;
		} catch(Exception e) {
			System.out.println(e);
			return JSP_CREATE;
		}
	}

	@RequestMapping(value = "/edit/{id}", method = RequestMethod.GET)
	public String update(
	HttpSession session,
	@PathVariable("id") Integer id,
	Model model  ) {
		QuestionAnswer questionAnswer = questionAnswerService.findById(id);
		try {
			model.addAttribute("item", questionAnswer);
			model.addAttribute("activePage", nav);
			return JSP_EDIT;
		} catch(Exception e) {
			return JSP_LIST;
		}
	}
	
	@RequestMapping(value = "/edit/{id}", method = RequestMethod.POST)
	public String update(
	@RequestParam(value="edit",defaultValue="0") Integer edit,
	@ModelAttribute QuestionAnswer item,
	HttpSession session,
	@PathVariable("id") Integer id,
	Model model  ) {
		QuestionAnswer questionAnswer = questionAnswerService.findById(id);
		try {
			questionAnswer.setProgramId( item.getProgramId() );
			questionAnswer.setAskUserId( item.getAskUserId() );
			questionAnswer.setAnswerUserId( item.getAnswerUserId() );
			questionAnswer.setQuestioin( item.getQuestioin() );
			questionAnswer.setAnswer( item.getAnswer() );
			questionAnswer.setAskDate( item.getAskDate() );
			questionAnswer.setUpdateDate( DataUtils.getNowDate() );
			questionAnswer.setAnswerDate( item.getAnswerDate() );
			questionAnswer = questionAnswerService.update(questionAnswer);
			model.addAttribute("item", questionAnswer);
		} catch(Exception e) {
			System.out.println(e);
		}
		return "redirect:/"+JSP_EDIT+"/"+id;
	}
	
	

	@RequestMapping(value = "/delete/{id}")
	public String delete(
	Model model, 
	HttpSession session,
	@PathVariable("id") Integer id ) {
	questionAnswerService.delete(id);
	return "redirect:/"+JSP_LIST;
	}

}
